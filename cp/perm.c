#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main(void) {
	int n;
	printf("Enter Number: ");
	scanf("%d", &n);

	if (n > 72 || n < 1) {
		printf("Invalid Input\n");
		return 1;
	}

	int a, b;
}
